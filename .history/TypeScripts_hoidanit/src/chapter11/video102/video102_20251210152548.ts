export {};

type TUsername = string | number;

type TStudent = {
    id: string;
    name: string;
}

type TCoder = {
    address: string;
    language
}

let username: TUsername = "hoidanit";
username = 12345;