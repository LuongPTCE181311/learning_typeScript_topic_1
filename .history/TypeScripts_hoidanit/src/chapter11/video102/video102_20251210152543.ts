export {};

type TUsername = string | number;

type TStudent = {
    id: string;
    name: string;
}

type TCoder = {
    address: string;
    la
}

let username: TUsername = "hoidanit";
username = 12345;